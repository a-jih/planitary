<html>
<head>
	<title>Groups | Planitary</title>
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
	<header>
	</header>
	<main>
	</main>
	<footer>
		<nav>
			{{#each pages}}
			<a href="{{link}}">
				<img src="/images/{{icon}}" alt="{{title}} Page">
				<p>{{title}}</p>
			</a>
			{{/each}}
		</nav>
	</footer>
</body>
</html>
