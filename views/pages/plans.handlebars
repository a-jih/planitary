{{!-- pages/plans --}}

{{#> layouts/app page-title="Plan It" sub="Plan It!"}}

	{{#*inline "main-block"}}
		{{#each groups}}
			<div class="content-card grpplans">
				<header>
    			<h3>{{name}}</h3>
  			</header>
				<div>
				{{> includes/group-plans gid=id}}
				</div>
			</div>
		{{/each}}
	{{/inline}}

{{/layouts/app}}